import {FETCH_PHONES_START, FETCH_PHONES_SUCCESS,FETCH_PHONES_FAILURE} from 'actionTypes'


const fetchPhones = () => aysnc dispatch =>{

	try{
	  dispatch({FETCH_PHONES_START})
	  const phones = await fetchPhonesApi()
	  dispatch({type:FETCH_PHONES_SUCCESS,
				payload:phones
			   })
	}catch(err){
		dispatch({
			type:FETCH_PHONES_FAILURE,
			payload:err,
			error:true
		})
	}
}